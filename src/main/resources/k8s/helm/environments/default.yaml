# Default environment values (local dev / minikube)
# Secrets must be passed via environment variables — never hardcode them here.
#
# Required env vars before running helmfile apply:
#   export MYSQL_ROOT_PASSWORD=...
#   export KEYCLOAK_CLIENT_SECRET=...
#   export VAULT_TOKEN=...
#   export AWS_SECRET_ACCESS_KEY=test        # localstack dev value
#   export STRIPE_API_KEY=sk_test_...
#   export STRIPE_WEBHOOK_SECRET=whsec_...
